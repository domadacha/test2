name: test2
run-name: Build `test2` with `test2` ⛵️
on: [ push ]

jobs:
  build:
    runs-on: linux-runner-prod
    steps:
      - uses: actions/checkout@v3
      - uses: domadacha/test2@main
        with:
          verb: 'build'
